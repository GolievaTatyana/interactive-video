function visibleButtons() {
  var buttons = document.getElementById("btn");
  buttons.classList.add("visible");
};

// var video = document.getElementById("video")[0];
// if (video) {
//   video.addEventListener("ended", visibleButtons);
// }
